index=web_logs earliest=-15m (".env" OR "config.php" OR "database.yml" OR "wp-config.php" OR ".git/config" OR ".htaccess")
| stats count dc(uri) as unique_targets by clientip, user_agent
| where count > 1
| sort -count
