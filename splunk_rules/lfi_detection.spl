index=web_logs earliest=-10m ("../" OR "..\\" OR "/etc/passwd" OR "boot.ini" OR "php://filter" OR "php://input")
| stats count dc(uri) as unique_targets by clientip, user_agent
| where count > 2
| sort -count
