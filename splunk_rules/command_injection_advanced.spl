index=web_logs earliest=-5m ("wget http" OR "curl http" OR "bash -i" OR "sh -i" OR "nc -" OR "/dev/tcp/" OR "cmd.exe" OR "powershell -nop")
| stats count dc(uri) as unique_targets by clientip, method, user_agent
| where count > 2 OR unique_targets > 1
| sort -count
