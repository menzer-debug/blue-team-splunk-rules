index=web_logs earliest=-10m ("sqlmap" OR "nikto" OR "nmap" OR "masscan" OR "acunetix" OR "curl" OR "python-requests")
| stats count dc(uri) as unique_targets by clientip, user_agent
| where count > 2 OR unique_targets > 2
| sort -count
