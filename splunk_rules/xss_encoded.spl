index=web_logs earliest=-5m ("%3Cscript%3E" OR "%3Cimg" OR "javascript%3A" OR "%3Csvg" OR "onerror%3D" OR "onload%3D")
| stats count dc(uri) as unique_targets by clientip, user_agent
| where count > 2 OR unique_targets > 1
| sort -count
